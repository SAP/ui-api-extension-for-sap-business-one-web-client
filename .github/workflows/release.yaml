name: Release

on:
  release:
    types: [published]

jobs:
  build:
    name: Build and Test
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0

      - name: Set up Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Package extension
        run: npm run package

      - name: Attach VSIX to GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          vsix=$(ls *.vsix | head -n 1)
          gh release upload ${{ github.event.release.tag_name }} "$vsix" --clobber

      - name: Upload VSIX artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: vsix-package
          path: "*.vsix"
          if-no-files-found: error

  # ─── Publish the VS Code extension to the Visual Studio Marketplace ─────
  # vscode-publish:
  #   name: Publish to VS Code Marketplace
  #   needs: [build]
  #   environment: "vscode:SAPOSS.ui-api-extension-for-sap-business-one-web-client"
  #   runs-on: ubuntu-latest
  #   permissions:
  #     contents: read
  #     id-token: write  # required for OIDC / managed identity login
  #   steps:
  #     - name: Set up Node
  #       uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
  #       with:
  #         node-version: 24

  #     - name: Download artifact
  #       uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
  #       with:
  #         name: vsix-package

  #     - name: Azure user-assigned managed identity login
  #       uses: azure/login@532459ea530d8321f2fb9bb10d1e0bcf23869a43 # v3.0.0
  #       with:
  #         client-id: "57788a52-1201-491a-9744-81be6eaba61c"
  #         tenant-id: "69b863e3-480a-4ee9-8bd0-20a8adb6909b"
  #         allow-no-subscriptions: true

  #     - name: Publish to Visual Studio Marketplace
  #       run: npx @vscode/vsce publish --azure-credential --packagePath *.vsix
